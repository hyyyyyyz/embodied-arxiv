# Copy this file to .env and fill in your real key for LOCAL development only.
# The real .env file is gitignored and will never be committed.
# In GitHub Actions, the same variable is read from Repository Secrets — do NOT put real keys here.

DEEPSEEK_API_KEY=your_deepseek_api_key_here
