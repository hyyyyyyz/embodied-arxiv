---
title: "提出分布化评估方法HRT和KS检验的具身VLA基准"
sidebar: false
aside: false
outline: false
---

<a class="back-btn" href="./">← 返回 2026-05-29</a>

# 提出分布化评估方法HRT和KS检验的具身VLA基准

### PhAIL: A Real-Robot VLA Benchmark and Distributional Methodology

<div class="paper-meta-row">
<span class="badge badge-verdict">👀</span>
<span class="badge badge-score">⭐ 10.0</span>
<span class="badge badge-topic" style="background:#7c3aed22;color:#7c3aed">VLA</span>
<span class="paper-id">2605.29710</span>
</div>

<div class="paper-links">📄 <a href="http://arxiv.org/abs/2605.29710v1">arXiv 摘要页</a> &nbsp;·&nbsp; 📑 <a href="https://arxiv.org/pdf/2605.29710v1">PDF 全文</a></div>

*Sergey Arkhangelskiy*

![framework](/figures/2026-05-29/2605.29710.png)

<small>Figure 1: (a) Time-to-success CDFs are richer than any scalar: reliability and throughput on a single axis. The four VLAs all sit far below the human reference – the best is ∼7× slower. Hard failures become the asymptote below F = 1. (b) Ch</small>

## 💡 关键 Tricks

- <span class="trick-core">⭐ 核心</span> — **以时间-成功累积分布函数(CDF)替代固定超时的二值成功率，保留时间维度信息，提升统计分辨率。**
- 引入Human-Relative Throughput (HRT)标量指标，以人类遥操作为锚点，结合bootstrap置信区间，实现无量纲可比性。
- 采用Kolmogorov-Smirnov检验进行每对象显著性测试，再宏平均跨对象，在N≤30时能分辨二值指标无法区分的模型对。
- 开源完整数据集、每次rollout的工件和端到端参考实现，确保可复现性。

## 📝 摘要

真实世界评估视觉-语言-动作(VLA)策略仍依赖固定超时的二值成功率，每个条件通常N≤25次rollout，几乎不带置信区间或配对统计比较；这些样本量难以可靠分辨接近的比较。我们提出PhAIL（物理AI排行榜，https://phail.ai），一个基于Franka FR3的开放真实机器人基准（包含数据集、每次rollout的工件和端到端参考实现），采用分布化评估方法：以时间-成功累积分布函数(CDF)作为评估原语，分为两个独立任务。第一个是通过Human-Relative Throughput (HRT)评分，这是一个无量纲标量，带有bootstrap置信区间，锚定于同设备的人类遥操作。第二个是显著性检验（Kolmogorov-Smirnov检验，按对象计算并宏平均跨对象）。在四个公开VLA上，宏平均KS检验在每(模型,对象)单元N≤30次rollout下分辨了两组接近的比较（GR00T vs. ACT, OpenPI vs. ACT），而二值阈值指标无法做到；最接近的一对（OpenPI vs. GR00T）在我们的预算内仍未分辨。评估的最佳VLA比人类参考慢约7倍（RMST比率）。

<details>
<summary>English (原文)</summary>

Real-world evaluation of vision-language-action (VLA) policies still rests on binary success rate at a fixed timeout with $N \le 25$ rollouts per condition, almost always without confidence intervals or paired statistical comparison; these cohort sizes struggle to resolve close comparisons reliably. We introduce PhAIL (Physical AI Leaderboard, https://phail.ai), an open real-robot benchmark on a Franka FR3 (dataset, per-rollout artifacts, and end-to-end reference implementation) of a distributional evaluation methodology: the time-to-success cumulative distribution function (CDF) as the evaluation primitive, with two separated jobs. The first is scoring via Human-Relative Throughput (HRT), a dimensionless scalar with bootstrap confidence intervals, anchored to same-fixture human teleoperation. The second is a significance test (Kolmogorov-Smirnov, computed per-object and macro-averaged across objects). On four publicly-available VLAs, the macro-averaged KS test resolves two close comparisons (GR00T vs. ACT, OpenPI vs. ACT) at $N \le 30$ rollouts per (model, object) cell where binary-threshold metrics do not; the closest pair (OpenPI vs. GR00T) remains unresolved within our budget. The best evaluated VLA is $\sim 7\times$ slower per operation (RMST ratio) than the human reference.

</details>

## 🔗 与已有工作的关系

与现有VLA基准（如OpenVLA、RT-2等）依赖固定超时二值成功率不同，PhAIL引入时间-成功CDF和分布化统计检验，借鉴了生存分析思想。与ACT、GR00T等具体方法对比，展示了新指标在分辨接近模型时的优势。

<div class="critique-box">
<div class="critique-header"><span class="critique-emoji">👀</span> 锐评</div>
<div class="critique-body">分布化评估方法（CDF+HRT+KS检验）是对现有VLA基准统计缺陷的合理改进，实验也证明了其在N≤30时能分辨二值指标无法区分的模型对，方向正确。但KS检验在宏平均时可能掩盖对象间的异质性，且最佳VLA仍比人类慢7倍，说明当前模型效率远非实用。此外，基准仅基于Franka FR3单一平台，泛化性存疑。</div>
</div>

---

**Tags**: `vla` `benchmark` `real-robot` `distributional-evaluation` `statistical-testing`
