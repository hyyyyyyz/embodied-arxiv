---
title: "异构CPU仿真/GPU学习架构，解耦物理与策略更新"
sidebar: false
aside: false
outline: false
---

<a class="back-btn" href="./">← 返回 2026-05-29</a>

# 异构CPU仿真/GPU学习架构，解耦物理与策略更新

### A Heterogeneous Architecture for Robot RL Beyond GPU-Dominant Paradigms

<div class="paper-meta-row">
<span class="badge badge-verdict">👀</span>
<span class="badge badge-score">⭐ 9.0</span>
<span class="badge badge-topic" style="background:#0891b222;color:#0891b2">sim2real</span>
<span class="paper-id">2605.30313</span>
</div>

<div class="paper-links">📄 <a href="http://arxiv.org/abs/2605.30313v1">arXiv 摘要页</a> &nbsp;·&nbsp; 📑 <a href="https://arxiv.org/pdf/2605.30313v1">PDF 全文</a></div>

*Yufei Jia, Zhanxiang Cao, Mingrui Yu, Heng Zhang 等 51 位*

![framework](/figures/2026-05-29/2605.30313.png)

<small>Figure 2: UniLab system architecture. The figure shows the data, scheduling, and parameter-synchronization paths between CPU-side batched physics backends, the unified runtime, and the GPU learner. 3 UniLab Architecture</small>

## 💡 关键 Tricks

- <span class="trick-core">⭐ 核心</span> — **提出UniLab架构，通过统一运行时解耦CPU并行仿真与GPU策略更新，实现数据移动、缓冲和同步的高效管理。**
- 集成MuJoCoUni和MotrixSim两种CPU批处理物理后端，支持多种RL算法（PPO, SAC, FlashSAC, TD3, APPO）。
- 通过异步流水线设计，减少GPU等待时间，提升端到端训练效率3-10倍。
- 降低对NVIDIA CUDA软件栈的依赖，支持跨平台执行（macOS, AMD ROCm, Intel XPU）。

## 📝 摘要

基于仿真的机器人强化学习越来越倾向于将物理引擎、轨迹收集和学习全部放在GPU上执行。这种范式显著提升了训练速度，但也导致了一个默认假设：高效训练需要物理计算驻留在GPU上。我们重新审视了这一假设。我们认为，在仿真主导的机器人控制中，关键问题不在于哪个处理器运行物理，而在于仿真吞吐量、策略学习和运行时同步是否形成高效的端到端循环。我们提出了UniLab，一种异构CPU仿真/GPU学习架构，通过统一的数据移动、缓冲和同步运行时，将CPU并行仿真与GPU策略更新解耦。UniLab是一个完整且可扩展的训练系统，使用MuJoCoUni和MotrixSim CPU批处理物理后端，支持PPO、SAC、FlashSAC、TD3和APPO算法。在代表性的仿真机器人控制任务上，UniLab在相同硬件配置下将端到端训练效率提升了3-10倍，同时减少了对NVIDIA CUDA软件栈的依赖，并支持在Apple macOS平台以及AMD ROCm和Intel XPU加速后端上跨平台执行。这些结果表明，GPU仿真是高效训练的有效途径，但并非必要途径，从而拓宽了机器人强化学习训练的实际系统选择。

<details>
<summary>English (原文)</summary>

Simulation-based RL for contemporary robot control is increasingly organized around GPU-resident simulation: physics, rollout collection, and learning are placed on a single GPU-centric execution path. This paradigm has greatly improved training speed, but it has also encouraged a default assumption that efficient training requires physics to reside on the GPU. We revisit this assumption. Our view is that, in simulation-dominated robot control, the essential question is not which processor runs physics, but whether simulation throughput, policy learning, and runtime synchronization form an efficient end-to-end loop. We present UniLab, a heterogeneous CPU-simulation / GPU-learning architecture that decouples CPU-parallel simulation from GPU policy updates through a unified runtime for data movement, buffering, and synchronization. UniLab is implemented as a complete and extensible training system using MuJoCoUni and MotrixSim CPU-batched physics backends, supporting PPO, SAC, FlashSAC, TD3, and APPO. On representative simulation-based robot control tasks, UniLab improves end-to-end training efficiency by 3--10$\times$ under the same hardware configuration, while reducing dependence on the NVIDIA CUDA-based software stack and supporting cross-platform execution on the Apple macOS platform and the AMD ROCm and Intel XPU accelerator backends. These results show that GPU simulation is an effective path to efficient training, but not a necessary one, broadening the practical system choices available for robot RL training. Project page: https://github.com/unilabsim/UniLab.

</details>

## 🔗 与已有工作的关系

与现有GPU中心化的仿真训练范式（如Isaac Gym、Brax）不同，UniLab挑战了物理必须运行在GPU上的默认假设，通过CPU批处理仿真和GPU学习解耦实现高效训练。与传统的CPU仿真+GPU学习架构（如MuJoCo+PyTorch）相比，UniLab通过统一运行时优化了数据移动和同步，显著提升了吞吐量。

<div class="critique-box">
<div class="critique-header"><span class="critique-emoji">👀</span> 锐评</div>
<div class="critique-body">论文挑战了具身智能领域一个被默认接受的假设（GPU仿真必须），并给出了一个工程上扎实的替代方案，效率提升3-10倍的数据很有说服力。但核心贡献更多是系统层面的工程优化，而非算法或理论创新，且实验仅在仿真环境中进行，未提及真实机器人部署。此外，CPU批处理仿真在复杂场景下的可扩展性（如大规模并行环境）是否仍能保持优势，摘要未给出明确分析。</div>
</div>

---

**Tags**: `reinforcement-learning` `simulation` `heterogeneous-architecture` `robot-control` `cpu-simulation`
