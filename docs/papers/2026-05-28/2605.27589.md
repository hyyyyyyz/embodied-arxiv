---
title: 因果世界模型基准，测试视频生成对物理变化的敏感性。
hide:
  - navigation
---

<a class="back-btn" href="../">← 返回 2026-05-28</a>

# 因果世界模型基准，测试视频生成对物理变化的敏感性。

### What-If World: A Causal Benchmark for General World Models in Embodied Scenarios

<div class="paper-meta-row">
<span class="badge badge-verdict">👀</span>
<span class="badge badge-score">⭐ 8.0</span>
<span class="badge badge-topic" style="background:#9333ea22;color:#9333ea">world-model</span>
<span class="paper-id">2605.27589</span>
</div>

<div class="paper-links">📄 <a href="http://arxiv.org/abs/2605.27589v1">arXiv 摘要页</a> &nbsp;·&nbsp; 📑 <a href="https://arxiv.org/pdf/2605.27589v1">PDF 全文</a></div>

*Kunlin Cai, Rui Song, Jinghuai Zhang, Kaiyuan Zhang 等 10 位*

<figure markdown>
  ![framework](/figures/2026-05-28/2605.27589.png)
  <figcaption>Figure 1: What-If World benchmark overview. Three stages address the challenges of evaluating the causal sensitivity of video world models: (1) what to test, (2) how to test, and (3) how to evaluate.</figcaption>
</figure>

## 💡 关键 Tricks

- <span class="trick-core">⭐ 核心</span> — **提出APEO四维评分（Adherence, Physics, Environment, Outcome），分别评估视频对提示的遵循、物理一致性、场景保持和因果结局，比单视频指标更全面。**
- 构建319对提示对，每对仅改变一个物理变量（如物体位置、速度），基于真实帧（nuScenes/DROID）并覆盖驾驶和操作场景。
- 设计六类物理变量（如碰撞、遮挡、重力）的因果干预分类法，系统评估模型对不同物理规律的敏感性。
- 发现模型性能与干预的视觉显著性正相关（如大物体移动得分40.4% vs 小物体14.2%），揭示模型依赖外观而非物理推理。

## 📝 摘要

视频生成模型越来越多地被用作世界模拟器，用于驾驶和机器人操作等任务。在这些场景中，关键不在于单个视频看起来是否正确，而在于当输入改变时模型的输出是否随之改变。我们通过给模型两个描述同一场景但仅改变一个物理细节的提示，并检查两个视频是否按照物理预测的方式产生分歧来测试这一点。提示之间的措辞差异很小（因为只改变了一个变量），但正确的物理差异却不小。忽略这一点的模型仍然可以生成两个各自看起来合理的视频，而现有基准一次只评分一个视频，无法检测到这种失败。我们引入了What-If World，这是一个包含319个这样的提示对的数据集，基于nuScenes和DROID的真实帧构建，并按驾驶和操作共享的六类物理变量分类。每个提示对使用APEO评分，这是一个四部分评估标准，检查每个视频是否遵循其提示（Adherence）、物理一致（Physics）、保持共享场景（Environment）以及最终产生正确的差异（Outcome）。在九个最先进的模型中，没有一个在配对评分上超过52%，开源模型集中在28%附近。每个测试模型在大量因果干预上都失败，表明这些模型在可靠支持动作条件模拟或基于模型的规划之前还有很大差距。在模型得分较高的地方，性能似乎与干预的视觉显著性相关，而非其底层物理的可处理性。一些视觉上微妙的干预得分低至14.2%，而视觉上显著的干预得分可达40.4%。

<details>
<summary>English (原文)</summary>

Video generation models are increasingly used as world simulators for tasks like driving and robotic manipulation. What matters in these settings is not whether a single video looks right, but whether the model's output changes when its input changes. We test this by giving a model two prompts describing the same scene with one physical detail varied, and checking whether the two videos diverge the way physics predicts. The wording difference between the prompts is small by design, since only one variable is changed, but the correct physical difference is not. A model that misses this can still produce two videos that each look plausible individually, and existing benchmarks score videos one at a time and cannot detect this failure. We introduce What-If World, 319 such prompt pairs built on real frames from nuScenes and DROID, organized by a taxonomy of six physical variables shared across driving and manipulation. Each pair is scored with APEO, a four-part rubric checking whether each video follows its prompt (Adherence), is physically consistent (Physics), preserves the shared scene (Environment), and ends in the correct difference (Outcome). Across nine state-of-the-art models, no system exceeds 52% on the paired score, and open-source models cluster near 28%. Every model tested fails on a large fraction of causal interventions, indicating substantial room before these models can reliably support action-conditioned simulation or model-based planning. Where models do score well, performance appears to track the visual prominence of the intervention rather than the tractability of its underlying physics. Some visually subtle interventions score as low as 14.2%, while visually pronounced ones reach 40.4%.

</details>
## 🔗 与已有工作的关系

与现有视频生成基准（如UCF101、Kinetics）不同，这些基准仅评估单视频质量，而What-If World通过成对提示测试因果敏感性。与物理推理基准（如CLEVRER、PHYRE）相比，本工作聚焦于真实世界视频生成模型，而非合成场景或符号推理。

<div class="critique-box">
<div class="critique-header"><span class="critique-emoji">👀</span> 锐评</div>
<div class="critique-body">基准设计精巧，APEO评分和因果干预分类法直击当前视频生成模型的核心缺陷——缺乏物理因果推理。但319个提示对规模偏小，且仅覆盖驾驶和操作两个领域，泛化性存疑。另外，模型在视觉显著干预上得分更高这一发现虽有趣，但未深入分析模型内部机制（如注意力是否集中在显著区域），略显表面。</div>
</div>

---

**Tags**: `world-model` `video-generation` `causal-benchmark` `embodied-ai`
