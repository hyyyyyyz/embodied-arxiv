---
title: 状态自适应噪声轨迹调度器，优化WAM动作生成效率
hide:
  - navigation
---

<a class="back-btn" href="../">← 返回 2026-05-28</a>

# 状态自适应噪声轨迹调度器，优化WAM动作生成效率

### SANTS: A State-Adaptive Scheduler for World Action Models

<div class="paper-meta-row">
<span class="badge badge-verdict">👀</span>
<span class="badge badge-score">⭐ 9.0</span>
<span class="badge badge-topic" style="background:#7c3aed22;color:#7c3aed">VLA</span>
<span class="paper-id">2605.27947</span>
</div>

<div class="paper-links">📄 <a href="http://arxiv.org/abs/2605.27947v1">arXiv 摘要页</a> &nbsp;·&nbsp; 📑 <a href="https://arxiv.org/pdf/2605.27947v1">PDF 全文</a></div>

*Yirui Sun, Guangyu Zhuge, Keliang Liu, Jie Gu 等 7 位*

<figure markdown>
  ![framework](../../assets/figures/2026-05-28/2605.27947.png)
  <figcaption>Figure 2: Overview of SANTS. SANTS is attached to a frozen video–action diffusion policy. During video denoising, it reads the current video-state representation and noise level, and jointly predicts a stopping hazard and a relative noise-p</figcaption>
</figure>

## 💡 关键 Tricks

- <span class="trick-core">⭐ 核心</span> — **提出状态自适应噪声轨迹调度器（SANTS），根据当前视频状态和噪声水平动态选择去噪深度，而非固定全去噪，从而在动作质量与推理成本间取得平衡。**
- 使用累积停止风险函数和相对噪声进展比联合预测最优停止点，实现轻量级调度。
- 采用路径级奖励进行后训练，奖励基于下游动作质量而非视频保真度，并显式惩罚冗余视频状态更新。
- 通过控制去噪深度扫描实验，揭示视频细化对动作误差存在状态依赖的饱和甚至反转现象，为自适应调度提供动机。

## 📝 摘要

=== "中文"

    世界动作模型（WAM）通过使用基于视频的未来表示来条件化动作生成，从而改进机器人操作。然而，在像素空间WAM中，最佳动作条件不一定是完全去噪的视频。受控的去噪深度扫描显示，视频细化可以降低动作误差，但存在一个状态依赖的临界点，之后当后期预测变得与动作相关性降低或物理上不可靠时，增益可能饱和甚至反转。这表明动作生成应使用沿视频噪声轨迹的状态依赖点，而非固定的终端去噪深度。我们引入了状态自适应噪声轨迹调度器（SANTS），一种用于视频到动作扩散策略的轻量级调度器。在每个视频决策点，SANTS读取当前视频状态表示和噪声水平，然后联合预测累积停止风险函数和相对噪声进展比。SANTS通过路径级奖励进行后训练，该奖励在冻结的动作分支生成最终动作块后计算，因此调度器针对下游动作质量而非中间视频保真度进行优化，同时显式惩罚冗余的视频状态更新。实验表明，SANTS在RoboTwin 2.0上达到94.4%的整体成功率，在七个真实机器人任务上平均成功率为73.1%，同时相对于全视频去噪分别降低了81.7%和79.0%的延迟。这些结果表明，沿视频噪声轨迹的自适应选择可以保留WAM风格未来推理的控制优势，同时消除其大部分冗余推理成本。

=== "English"

    World Action Models (WAMs) improve robot manipulation by using video-based future representations to condition action generation. In pixel-space WAMs, however, the best action condition is not necessarily the fully denoised video. Controlled denoising-depth scans show that video refinement can reduce action error up to a state-dependent point, after which the gain may saturate or even reverse when late predictions become less action-relevant or physically unreliable. This suggests that action generation should use a state-dependent point along the video noise trajectory rather than a fixed terminal denoising depth. We introduce State-Adaptive Noise Trajectory Scheduler (SANTS), a lightweight scheduler for video-to-action diffusion policies. At each video decision point, SANTS reads the current video-state representation and noise level, then jointly predicts a cumulative stopping hazard and a relative noise-progression ratio. SANTS is post-trained with a path-level reward computed after the frozen action branch generates the final action chunk, so the scheduler is optimized for downstream action quality rather than intermediate video fidelity, while redundant video-state updates are explicitly penalized. Experiments show that SANTS reaches \(94.4\%\) overall success on RoboTwin 2.0 and \(73.1\%\) average success across seven real-robot tasks, while reducing latency by \(81.7\%\) and \(79.0\%\) relative to full video denoising, respectively. These results indicate that adaptive selection along the video noise trajectory can preserve the control benefits of WAM-style future reasoning while removing much of its redundant inference cost.

## 🔗 与已有工作的关系

与现有WAM方法（如使用全去噪视频作为动作条件）不同，SANTS通过自适应选择去噪深度来减少冗余推理。与传统扩散策略的固定步数调度相比，SANTS利用状态信息动态调整，更高效。与视频预测模型相比，SANTS直接优化动作质量而非视频保真度。

<div class="critique-box">
<div class="critique-header"><span class="critique-emoji">👀</span> 锐评</div>
<div class="critique-body">核心insight清晰：视频去噪并非越深越好，存在状态依赖的饱和点。SANTS的轻量级调度器设计简洁有效，实验在仿真和真实任务上均展示了显著延迟降低且保持高成功率。但论文未充分讨论调度器对动作质量的理论保证，例如在什么条件下自适应调度优于固定深度？此外，路径级奖励依赖冻结的动作分支，若动作分支更新，调度器是否需要重新训练？</div>
</div>

---

**Tags**: `world-action-model` `diffusion-policy` `adaptive-scheduling` `robot-manipulation`
