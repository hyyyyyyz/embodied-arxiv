---
title: 用策略比率方差正则化替代PPO的硬裁剪，实现稳定且高效的策略优化。
hide:
  - navigation
---

<a class="back-btn" href="../">← 返回 2026-05-28</a>

# 用策略比率方差正则化替代PPO的硬裁剪，实现稳定且高效的策略优化。

### Ratio-Variance Regularized Policy Optimization

<div class="paper-meta-row">
<span class="badge badge-verdict">👀</span>
<span class="badge badge-score">⭐ 8.0</span>
<span class="badge badge-topic" style="background:#dc262622;color:#dc2626">policy-learning</span>
<span class="paper-id">2605.26784</span>
</div>

<div class="paper-links">📄 <a href="http://arxiv.org/abs/2605.26784v1">arXiv 摘要页</a> &nbsp;·&nbsp; 📑 <a href="https://arxiv.org/pdf/2605.26784v1">PDF 全文</a></div>

*Yu Luo, Shuo Han, Yihan Hu, Lei Lv 等 8 位*

<figure markdown>
  ![framework](../../assets/figures/2026-05-28/2605.26784.png)
  <figcaption>Figure 10. Visualizations of the continuous control benchmarks. We evaluate our method on 10 diverse tasks from the DeepMind Control Suite, ranging from simple balancing tasks (e.g., Cartpole) to complex locomotion tasks (e.g., Humanoid, Ch</figcaption>
</figure>

## 💡 关键 Tricks

- <span class="trick-core">⭐ 核心</span> — **提出用策略比率方差作为信任区域的局部近似约束，替代PPO的硬裁剪，避免截断高回报但高散度的更新。**
- 采用原始-对偶优化框架实现方差约束，自动调节约束强度，无需手动调整裁剪阈值。
- 方差正则化作为分布式的“软刹车”，保留新颖探索的梯度信号，同时自然降低旧数据权重，允许复用off-policy数据。

## 📝 摘要

=== "中文"

    标准的在线强化学习依赖于启发式的裁剪来强制执行信任区域，但这种机制会不加区分地截断高回报但高散度的更新，造成严重损失。我们证明，显式约束策略比率方差为信任区域约束提供了原则性的局部近似，消除了二元硬裁剪的需求。通过作为分布式的“软刹车”，该方法在保留来自新颖发现的梯度信号的同时，自然地降低权重并允许复用陈旧的off-policy数据。我们提出了R²VPO（比率方差正则化策略优化），通过原始-对偶优化框架实现这一约束。在7种LLM规模（涵盖快慢推理范式）和10个机器人控制任务上的广泛评估证明了该方法的通用性。R²VPO在数学推理基准上取得了显著的性能提升，特别是在小模型上提升更为明显，同时显著提高了样本效率。此外，在连续控制领域，尤其是在稀疏奖励和动态环境中，它持续优于PPO基线。这些发现共同确立了比率方差正则化作为稳定且数据高效的策略优化的原则性基础。

=== "English"

    Standard on-policy reinforcement learning relies on heuristic clipping to enforce trust regions, but this mechanism imposes a severe cost by indiscriminately truncating high-return yet high-divergence updates. We demonstrate that explicitly constraining the policy ratio variance provides a principled local approximation to trust-region constraints, eliminating the need for binary hard clipping. By acting as a distributional ``soft brake'', this approach preserves critical gradient signals from novel discoveries while naturally down-weighting and enabling the reuse of stale, off-policy data. We introduce ${\bf R}^2{\bf VPO}$ (Ratio-Variance Regularized Policy Optimization), which implements this constraint via a primal-dual optimization framework. Extensive evaluations across $7$ LLM scales, spanning both fast and slow reasoning paradigms, and $10$ robotic control tasks demonstrate the generality of the proposed approach. R$^2$VPO achieves substantial performance gains on mathematical reasoning benchmarks, with particularly pronounced improvements on smaller models, while significantly improving sample efficiency. Furthermore, it consistently outperforms PPO baselines in continuous control domains, particularly in sparse-reward and dynamic environments. Together, these findings establish ratio-variance regularization as a principled foundation for stable and data-efficient policy optimization.

## 🔗 与已有工作的关系

与PPO的启发式裁剪不同，R²VPO通过显式约束策略比率方差来近似信任区域，避免了硬裁剪带来的信息损失。与TRPO等基于KL散度的方法相比，方差约束计算更简单且可微，便于原始-对偶优化。该方法还借鉴了off-policy复用思想，但通过方差正则化自然实现重要性采样权重调整。

<div class="critique-box">
<div class="critique-header"><span class="critique-emoji">👀</span> 锐评</div>
<div class="critique-body">方差约束作为信任区域近似在理论上很优雅，但原始-对偶优化可能引入额外的超参数（如拉格朗日乘子学习率），文中未充分讨论其敏感性。机器人实验仅涉及10个任务，且未与SOTA的off-policy方法（如SAC）对比，说服力有限。LLM实验虽然规模多样，但数学推理基准的改进是否来自方差正则化而非其他训练细节（如学习率调度）仍需消融验证。</div>
</div>

---

**Tags**: `reinforcement-learning` `policy-optimization` `trust-region` `ppo` `robotics`
