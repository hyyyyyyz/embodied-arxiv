---
title: 探针实验揭示冻结VLA编码成功信息，可提升策略性能。
hide:
  - navigation
---

<a class="back-btn" href="../">← 返回 2026-05-28</a>

# 探针实验揭示冻结VLA编码成功信息，可提升策略性能。

### What Frozen VLAs Already Know About Success: A Probing Study of Value-Like Structure in Foundation Robot Policies

<div class="paper-meta-row">
<span class="badge badge-verdict">👀</span>
<span class="badge badge-score">⭐ 9.0</span>
<span class="badge badge-topic" style="background:#7c3aed22;color:#7c3aed">VLA</span>
<span class="paper-id">2605.28527</span>
</div>

<div class="paper-links">📄 <a href="http://arxiv.org/abs/2605.28527v1">arXiv 摘要页</a> &nbsp;·&nbsp; 📑 <a href="https://arxiv.org/pdf/2605.28527v1">PDF 全文</a></div>

*Jiachen Zhang, Junnan Nie, Junyi Lao, Wei Cheng 等 7 位*

<figure markdown>
  ![framework](/figures/2026-05-28/2605.28527.png)
  <figcaption>Figure 1: Overview of the probing-to-selection pipeline. Frozen VLA and visual representations are probed for value-like signal on offline trajectories, then tested under same-task, same-timestep matched controls that hold task identity and</figcaption>
</figure>

## 💡 关键 Tricks

- <span class="trick-core">⭐ 核心</span> — **使用轻量线性探针从冻结的VLA特征中预测蒙特卡洛结果目标，无需重新训练策略。**
- 通过同任务、同时间步的匹配比较消除任务和时间捷径，确保探针学到的是真正的成功信号。
- 将探针作为测试时选择器，对Pi0.5的动作前缀进行排序，提升成功率的实验设计。
- 在多个视觉基础模型（OpenVLA, Pi0.5, DINOv2, CLIP）上验证了成功信息的可预测性，增强了结论的泛化性。

## 📝 摘要

视觉-语言-动作（VLA）策略被训练来模仿动作；它们的损失函数从未要求它们估计奖励、进展或未来成功。然而，它们的冻结表示仍然携带此类信息，并且可以通过轻量线性探针从冻结特征中读出，用于指导动作选择而无需重新训练策略。从LIBERO-Goal上的混合成功和失败操作轨迹中，我们使用轻量线性探针恢复蒙特卡洛结果目标。这些目标可以从OpenVLA、Pi0.5、DINOv2和CLIP特征中一致地预测，而基于进展、剩余时间、任务身份或本体感觉的基线则预测能力显著较弱。为了排除任务和时间捷径，我们在同任务、同时间步的匹配比较下评估探针：Pi0.5探针仍然达到约92%的成对排序准确率，而标签打乱的对照保持在随机水平。当作为测试时选择器对采样的Pi0.5动作前缀进行排序时，相同的探针将这一离线发现转化为行为：在推板任务上，成功率从贪婪解码的26.7%提升到44.3%，在酒架任务上也有一个正面案例。这些增益并非普遍适用，且需要额外的推理计算，但核心发现是清晰的：冻结的VLA已经编码了关于成功的信息，而它们的模仿目标从未明确要求这一点。

<details>
<summary>English (原文)</summary>

Vision--language--action (VLA) policies are trained to imitate actions; their loss never asks them to estimate reward, progress, or future success. Their frozen representations nevertheless carry such information, and it can be read out and used to guide action choice without retraining the policy. From mixed successful and failed manipulation trajectories on LIBERO-Goal, we recover Monte-Carlo outcome targets using lightweight linear probes on frozen features. The targets are consistently predictable from OpenVLA, Pi0.5, DINOv2, and CLIP features, and substantially less so from baselines built on progress, time-to-go, task identity, or proprioception. To rule out task and temporal shortcuts, we evaluate the probes under same-task, same-timestep matched comparisons: Pi0.5 probes still reach roughly 92% pairwise ordering accuracy, while label-shuffled controls stay at chance. Used as a test-time selector over sampled Pi0.5 action prefixes, the same probe turns this offline finding into behavior: on push-plate, success rises from 26.7% under greedy decoding to 44.3%, with a second positive case on wine-rack. The gains are not universal and require additional inference compute, but the underlying finding is clean: frozen VLAs already encode information about success that their imitation objective never explicitly demands.

</details>
## 🔗 与已有工作的关系

与直接使用奖励或成功信号训练策略的方法不同，本文展示了冻结的VLA特征中隐含成功信息，可通过轻量探针提取。这与OpenVLA、Pi0.5等VLA策略的常见用法（直接微调或零样本）形成对比，提供了一种无需重训即可利用策略内部知识的新视角。

<div class="critique-box">
<div class="critique-header"><span class="critique-emoji">👀</span> 锐评</div>
<div class="critique-body">实验设计干净，消融充分（同任务同时间步匹配、标签打乱对照），结论有说服力。但测试时选择器带来的性能提升有限（推板从26.7%到44.3%），且需要额外推理计算，实际部署价值存疑。另外，探针在更多任务上的泛化性未充分验证，仅两个任务有行为增益。</div>
</div>

---

**Tags**: `vla` `probing` `success-prediction` `test-time-adaptation` `imitation-learning`
